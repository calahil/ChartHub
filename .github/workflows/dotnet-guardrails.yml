name: Dotnet Guardrails

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  guardrails:
    uses: ./.github/workflows/dotnet-quality.yml
    with:
      dotnet_version: 10.0.x
      coverage_threshold: 70
      run_backupapi_tests: false
